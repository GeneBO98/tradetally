NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tradervue_db
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# JWT
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRE=7d

# Email
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Frontend URL
FRONTEND_URL=http://localhost:5173

# File Upload (50MB limit for large CSV files)
MAX_FILE_SIZE=52428800

# External API Keys (Optional)

# Finnhub API Key - For real-time stock quotes and CUSIP resolution
# Get your free API key at: https://finnhub.io/register
# Free tier: 60 API calls/minute with built-in rate limiting (30 calls/second)
# Provides both market data and symbol resolution in one API
FINNHUB_API_KEY=

# Google Gemini API Key - For AI-powered CUSIP resolution (backup)
# Get your free API key at: https://aistudio.google.com/app/apikey
# Used as fallback when Finnhub cannot resolve CUSIP codes
GEMINI_API_KEY=