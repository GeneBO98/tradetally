# Application Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tradervue_db
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=7d

# Registration Control
# Controls who can sign up for the application
# Options: 'open' (default - anyone can sign up), 'approval' (admin must approve), 'disabled' (no signups)
REGISTRATION_MODE=open

# Email Configuration (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
EMAIL_FROM=noreply@example.com

# Frontend Configuration
FRONTEND_URL=http://localhost:5173
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000

# File Upload Configuration
MAX_FILE_SIZE=52428800

# External API Keys (Optional)

# Finnhub API Key - For real-time stock quotes and CUSIP resolution
# Get your free API key at: https://finnhub.io/register
# Free tier: 60 API calls/minute with built-in rate limiting (30 calls/second)
# Provides both market data and symbol resolution in one API
FINNHUB_API_KEY=

# Alpha Vantage API Key - For trade chart visualization
# Get your free API key at: https://www.alphavantage.co/support/#api-key
# Free tier: 25 API calls/day, 5 calls/minute for historical stock data
# Used for generating candlestick charts with trade entry/exit markers
ALPHA_VANTAGE_API_KEY=

# Google Gemini API Key - For AI-powered CUSIP resolution (backup)
# Get your free API key at: https://aistudio.google.com/app/apikey
# Used as fallback when Finnhub cannot resolve CUSIP codes
GEMINI_API_KEY=

# Legacy API Key (Deprecated - can be removed)
# OpenFIGI API Key - No longer used as of v2.1.0, replaced by Finnhub
# OPENFIGI_API_KEY=

# Analytics Configuration (Optional)
# Set this to your analytics domain (e.g., https://analytics.domain.com)
VITE_ANALYTICS_DOMAIN=
VITE_ANALYTICS_SITE_ID=