name: Watch for tag or branch deletions

on:
  delete:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  log_deletion:
    runs-on: ubuntu-latest
    steps:
      - name: Log deleted ref
        run: echo "Ref deleted: ${{ github.event.ref }}"
